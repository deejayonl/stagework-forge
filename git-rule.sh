curl -sX POST "http://127.0.0.1:19006/chat" -H "Content-Type: application/json" -d '{"message": "[SYSTEM DIRECTIVE - GIT PROTOCOL UPDATE] DJ has noted that your git commits contain duplicate task IDs for iterative fixes (e.g., multiple commits named `Task 8.2: ...`). \n\nFrom now on, if you need to make multiple iterative commits to achieve a single task in STATUS.md, you MUST use semantic versioning for the subtasks. For example, if you are working on Task 51.1 and need 3 commits, name them:\n- `Task 51.1.1: [First piece]`\n- `Task 51.1.2: [Second piece]`\n- `Task 51.1.3: [Final piece]`\n\nDo not repeat `Task X.Y` multiple times. Adopt this granular commit structure immediately."}'
